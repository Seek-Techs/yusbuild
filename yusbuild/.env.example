# Frontend environment variables (Vite).
# Copy to .env.local and adjust as needed.

# Base URL for backend API requests.
# Default "/api" uses the Vite dev-server proxy to http://localhost:8000.
# Set to an absolute URL (e.g. https://api.yusbuild.ng/api) for deployed builds.
VITE_API_URL=/api
