import type { RouteObject } from "react-router-dom";

/**
 * Production stub for the component gallery.
 *
 * vite.config.ts aliases `@/dev/routes` here for production builds, so the
 * gallery screens and their fixtures never reach the shipped bundle.
 */
export const devRoutes: RouteObject[] = [];
